{
  "name": "aelig-tags",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "dfx generate backend && npm run start --workspaces --if-present",
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "local-canister": "dfx start",
    "local-deploy": "npm run build && dfx deploy",
    "generate": "dfx generate backend"
  },
  "type": "module",
  "workspaces": [
    "src/frontend"
  ],
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=7.0.0"
  }
}
